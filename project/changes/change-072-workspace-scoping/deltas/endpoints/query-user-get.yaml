# Your exact example: map has query [ ... ]; the effective view adds workspaceId. Planned, not live.
target: EP-USR-03
change: change-072
status: planned
reason: "Filter the users query by workspace"
# Spec of just the delta (inherits everything else from EP-USR-03 + profile.conventions):
spec:
  request:
    query: "adds ?workspaceId=<id> — optional; when present, results are scoped to that workspace"
  response: "unchanged (Paginated<UserDto>) but rows are workspace-filtered"
  auth: "adds WorkspaceRoleGuard when workspaceId is present (per conventions.api.workspace_auth)"
  errors:
    - "{ code: 403, when: caller is not a member of the requested workspace }"
patch:
  query:
    add: [workspaceId]
  rules:
    add: [RULE-WS-SCOPE-01]
