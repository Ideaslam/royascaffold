change: change-072
title: Workspace scoping for users
current_step: 3
steps:
  - { id: 1, action: update_entity_map,   target: ENT-USER,     status: done }
  - { id: 2, action: update_endpoint_map, target: EP-USR-03,    status: done }
  - id: 3
    action: implement
    target: ENT-USER
    archetype: entity.workspace-scoped
    files: [modules/users/schemas/user.schema.ts]
    rules: [RULE-WS-SCOPE-01]
    accept: "user.schema has workspaceId (indexed, ref Workspace); @map ENT-USER present; verify PASS"
    status: in_progress
  - id: 4
    action: implement
    target: EP-USR-03
    archetype: endpoint.crud-list
    files: [modules/users/users.controller.ts, modules/users/dto/list-users.query.ts]
    rules: [RULE-PAG-01, RULE-WS-SCOPE-01]
    calls: [SVC-USR.listUsers]
    accept: "GET /users accepts ?workspaceId and filters by it; @map EP-USR-03 present; verify PASS"
    status: planned
  - { id: 5, action: merge_deltas_to_map, status: planned }
